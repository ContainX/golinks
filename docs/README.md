# GoLinks Documentation

- `specs/` holds the product and technical specifications, numbered in reading order. Start with `specs/00-overview.md`.
- `decisions/` holds architecture decision records (ADRs).

The specs are the source of truth for behavior. When implementation and a spec disagree, fix one of them in the same change.
