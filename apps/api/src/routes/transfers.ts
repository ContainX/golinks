// Route module for the transfers endpoints of spec 05 §3. Registered by routes/api.ts.
// Empty until its task lands; keeping the registration point in place lets the
// composition and the app factory stay fixed while the endpoints are written.

import type { GoLinksApp } from '../types.ts'

export function registerTransferRoutes(_app: GoLinksApp): void {}
